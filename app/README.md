# Aplicación Java (antes `cloud_models_classifier`)

GUI y CLI del clasificador. El sitio del curso está en `/web`.

```bash
bash compile.sh
bash run-tests.sh
bash run-cli.sh "Correo electrónico en el navegador con suscripción mensual."
bash run-gui.sh
```

Para un monolito posterior: servir `web/` desde aquí y llamar a `classifier.ClassifierService`.
